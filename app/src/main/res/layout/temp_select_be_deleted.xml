<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".LoginActivity"
    android:id="@+id/screen"
    android:background="#b59dff">


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pulse Sensing"
        android:id="@+id/pulseButton"
        android:textColor="#ffffff"
        android:textSize="40sp"
        android:background="#b49cfe"
        android:layout_alignParentBottom="true"
        android:layout_alignLeft="@+id/doorButton"
        android:layout_alignStart="@+id/doorButton"
        android:layout_marginBottom="157dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Door SEnsing"
        android:id="@+id/doorButton"
        android:textColor="#ffffff"
        android:textSize="40sp"
        android:background="#b49cfe"
        android:layout_above="@+id/pulseButton"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="137dp" />
</RelativeLayout>
